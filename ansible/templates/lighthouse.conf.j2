server {
    listen          8123;
    server_name     *;
    access_log      /var/log/nginx/{{ lighthouse_access_log }}.log  main;

    location / {
        root        {{ lighthouse_dir }};
        index       index.html;
    }

}